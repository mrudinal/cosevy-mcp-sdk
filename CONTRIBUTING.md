# Contributing

Use one pull request per package when possible:

- `python-sdk/` for Python SDK changes.
- `javascript-sdk/` for TypeScript/JavaScript SDK changes.
- `mcp-server/` for MCP tools and transports.
- `docs/` for shared documentation.

Do not place source code in the repository root.
